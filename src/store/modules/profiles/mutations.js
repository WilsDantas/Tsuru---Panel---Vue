export default {
    SET_PROFILES(state, profiles) {
        state.profiles = profiles
    },
    SET_PROFILE(state, profile) {
        state.profile = profile
    },
    SET_ALL_PROFILES(state, allProfiles){
        state.allProfiles = allProfiles
    }
}