export default {
    SET_ORDERS(state, orders) {
        state.orders = orders
    },
    SET_ALL_ORDERS(state, orders) {
        state.Allorders = orders
    },
    SET_ORDER(state, order) {
        state.order = order
    },
}